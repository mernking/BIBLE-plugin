import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.bibleplugin',
  appName: 'BiblePluginMobile',
  webDir: 'www'
};

export default config;
